<!DOCTYPE html>
<html data-bs-theme='auto' lang='en'>
  <%= render 'layouts/head' %>

  <body>
    <% if controller_name == 'home' %>
      <%= yield %>
    <% else %>
      <%= render 'layouts/sidebar' %>
      <div class='content'>
        <div class='container'>
          <%= yield %>
        </div>
      </div>
    <% end %>
  </body>
</html>
