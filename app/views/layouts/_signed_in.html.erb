<div class="dropdown mb-2">
  <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
    <%= image_tag "avatar_#{current_user.role}.png", alt: current_user.email, width: 32, height: 32, class: "rounded-circle me-2" %>
    <strong>Admin</strong>
  </a>
  <ul class="dropdown-menu dropdown-menu-dark text-small shadow" style="">
    <li>
      <%= link_to user_profile_path, class: "dropdown-item" do %>
        Profile
      <% end %>
    </li>
    <li><hr class="dropdown-divider" /></li>
    <li>
      <%= link_to 'Sign out', destroy_user_session_path, method: :delete,
        data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?", turbo_method: :delete },
        class: "dropdown-item" %>
    </li>
  </ul>
</div>
