<%
  facilities = room.facilities.reorder(name: :asc).to_a
%>

<%= render 'shared/render_record_show', record: room do %>
  <% if action_name == "show" %>
    <div class="col-12">
      <hr />

      <h4 class='mb-3'>
        <%= Room.human_attribute_name(:facilities) %>
        <span class='badge bg-secondary'>
          <span><%= t('base.selected_text') %>: </span><span><%= facilities.count %></span>
        </span>
      </h4>

      <div class='row g-3 row-cols-1 row-cols-md-2 row-cols-xl-3'>
        <% facilities.each do |facility| %>
          <div class="col">
            <div class="alert mb-0 alert-secondary hover-shadow">
              <h5>
                <i class="bi bi-building"></i>
                <%= render_text_field(facility, :name) %>
              </h5>
              <p class='mb-0'>
                <%= render_text_field(facility, :summary) %>
              </p>
            </div>
          </div>
        <% end %>
      </div>

      <hr />
    </div>
  <% end %>
<% end %>
