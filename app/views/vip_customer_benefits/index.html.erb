<p style="color: green"><%= notice %></p>

<%
page_title = VipCustomerBenefit.model_name.human.pluralize(I18n.locale)
content_for :title, page_title
%>

<%= render partial: "shared/header_actions", locals: { title: page_title } %>

<div id="vip_customer_benefits" class="row row-cols-1 row-cols-md-2 row-cols-xl-3">
  <% @records.each do |vip_customer_benefit| %>
    <%= render vip_customer_benefit %>
  <% end %>
</div>

<%== pagy_bootstrap_nav(@pagy) %>
<p class='text-muted text-center'>
  <%== pagy_info(@pagy) %>
</p>

<%= link_to "New vip customer benefit", new_vip_customer_benefit_path, class: "btn btn-primary mt-3" %>
