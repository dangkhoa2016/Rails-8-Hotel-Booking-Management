<% content_for :title, "New vip customers benefit" %>

<%= render partial: "shared/header_actions", locals: { record: @vip_customers_benefit } %>

<%= render "form", vip_customers_benefit: @vip_customers_benefit %>
