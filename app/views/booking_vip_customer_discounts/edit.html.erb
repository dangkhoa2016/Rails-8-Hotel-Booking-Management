<% content_for :title, "Editing booking vip customer discount" %>

<%= render partial: "shared/header_actions", locals: { record: @booking_vip_customer_discount } %>

<%= render "form", booking_vip_customer_discount: @booking_vip_customer_discount %>
